echo "=== Checking What Happened ==="
echo ""
echo "1. Did you run the deploy script?"
ls -la ~/mcp-deployment-fix/deploy_mcp.sh 2>/dev/null || echo "Script not found in expected location"
echo ""
echo "2. Check if build was submitted:"
gcloud builds list --limit=2 --format="table(id,status,createTime,duration)"
echo ""
echo "3. Check if new revision was created:"
gcloud run revisions list --service=emailpilot-api --region=us-central1 --limit=5 --format="table(name,status,createTime)"
echo ""
echo "4. Current image being used:"
gcloud run services describe emailpilot-api --region=us-central1 --format="value(spec.template.spec.containers[0].image)"
echo ""
echo "If no recent builds or revisions with 'mcp' in name, deployment didn't run properly."